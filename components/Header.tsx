import React from 'react';

export const Header: React.FC = () => (
  <header className="py-4 px-4 md:px-6">
    <div className="container mx-auto">
      {/* Header content removed as per user request to create a cleaner look */}
    </div>
  </header>
);